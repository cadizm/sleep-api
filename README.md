### Description
FastAPI (requires Python3) providing endpoint that sleeps before returning (may be useful for debugging).

### Usage
```bash
$ pip install fastapi uvicorn
$ uvicorn --debug main:app
```

### Reference
* [docker hub](https://hub.docker.com/repository/docker/cadizm/sleep-api)
